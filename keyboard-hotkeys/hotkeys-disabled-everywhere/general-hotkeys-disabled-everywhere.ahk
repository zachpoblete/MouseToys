; Disable ^+w because accidentally pressing ^+w can close the window, and we
; already have !F4.
^+w::  return

; Disable #^d because accidentally pressing #^d creates a virtual desktop, and I
; don't need one.
#^d:: return

; Disable #c because accidentally pressing #c toggles Copilot, and I don't need
; it.
#c:: return
