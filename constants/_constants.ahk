; TODO: Instead of using global variables, use a class.
; Extend this class so that you don't get an error when running the individual files.

#Include general-constants.ahk
#Include zoom-constants.ahk
